<%= simple_form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :name %><br>
  </div>
  <div class="field">
    <%= f.association :place %><br>
  </div>
  <div class="field">
    <%= f.input :tag_list, collection: @tags %><br>
  </div>
  <div class="field">
    <%= f.input :avatar %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
